name: flutter_app
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: 2.0.5
  path: ^1.8.2
  just_audio: 0.9.27
  audio_service:
  wav_io: ^1.0.0

dev_dependencies:
  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/sounds/
